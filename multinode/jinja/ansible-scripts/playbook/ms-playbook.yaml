- hosts: ms
  vars:
    apigee_source_path: ../config
    apigee_dest_path: /tmp/apigee
    apigee_config_file_name: config.txt
  tasks:
  - name: Run Setup with MS profile
    command: "/opt/apigee/apigee-setup/bin/setup.sh -p ms -f {{apigee_dest_path}}/{{apigee_config_file_name}}"
    sudo: yes

